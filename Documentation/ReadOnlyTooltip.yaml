# ReadOnlyTooltip API Documentation
name: ReadOnlyTooltip
type: class
availability: client-only
description: ReadOnlyTooltip class

constructor:
  signature: ReadOnlyTooltip(var id)
  parameters:
    - name: id
      type: var
      description: id parameter
  returns: ReadOnlyTooltip instance

properties:
  - name: entity
    type: Entity
    access: read-only
    description: entity property

  - name: entityId
    type: uuid
    access: read-only
    description: entityId property

notes:
  - Auto-converted from HTML documentation
  - Client-only - not available on server