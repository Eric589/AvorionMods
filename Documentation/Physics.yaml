# Physics API Documentation
name: Physics
type: class
availability: both
description: Physics class

constructor:
  signature: Physics(var id)
  parameters:
    - name: id
      type: var
      description: id parameter
  returns: Physics instance

properties:
  - name: centerOfMass
    type: vec3
    access: read-only
    description: centerOfMass property

  - name: centerOfRotation
    type: vec3
    access: read-only
    description: centerOfRotation property

  - name: driftDecrease
    type: double
    access: read-only
    description: driftDecrease property

  - name: entity
    type: Entity
    access: read-only
    description: entity property

  - name: entityId
    type: uuid
    access: read-only
    description: entityId property

  - name: invMass
    type: double
    access: read-only
    description: invMass property

  - name: kineticEnergy
    type: double
    access: read-only
    description: kineticEnergy property

  - name: mass
    type: double
    access: read-only
    description: mass property

  - name: rotationalEnergy
    type: double
    access: read-only
    description: rotationalEnergy property

  - name: rotationDecrease
    type: double
    access: read-write
    description: rotationDecrease property

methods:
  - name: applyGlobalForce
    signature: applyGlobalForce(vec3 position, vec3 direction, float size, float time)
    parameters:
      - name: position
        type: vec3
        description: position parameter
      - name: direction
        type: vec3
        description: direction parameter
      - name: size
        type: float
        description: size parameter
      - name: time
        type: float
        description: time parameter
    returns: void
    description: applyGlobalForce method

  - name: applyImpulse
    signature: applyImpulse(dvec3 position, vec3 direction, float size)
    parameters:
      - name: position
        type: dvec3
        description: position parameter
      - name: direction
        type: vec3
        description: direction parameter
      - name: size
        type: float
        description: size parameter
    returns: void
    description: applyImpulse method

  - name: applyLocalForce
    signature: applyLocalForce(vec3 position, vec3 force, float size, float time)
    parameters:
      - name: position
        type: vec3
        description: position parameter
      - name: force
        type: vec3
        description: force parameter
      - name: size
        type: float
        description: size parameter
      - name: time
        type: float
        description: time parameter
    returns: void
    description: applyLocalForce method

  - name: lerpPositions
    signature: lerpPositions(double timeStep)
    parameters:
      - name: timeStep
        type: double
        description: timeStep parameter
    returns: void
    description: lerpPositions method

notes:
  - Auto-converted from HTML documentation