# Callbacks Documentation
name: Callbacks
type: callbacks
description: Callback functions for Callbacks scripts

callbacks:
  - name: onDialogClosed
    signature: onDialogClosed()
    description: This callback is executed when a dialog closes. Callback is executed at the script that provided the dialog. No need to register for this callback, just declare the function "onDialogClosed".

  - name: onShowEncyclopediaArticle
    signature: onShowEncyclopediaArticle(id)
    parameters:
      - name: id
        description: id parameter
    description: This is the official documentation for the scripting API of Avorion. This documentation is automatically generated and not necessarily complete. Depending on the context in which functions exist, some documentation such as descriptions, return values or variable names or types may be missing.

notes:
  - Manually converted from HTML callbacks documentation
  - Callbacks may be buffered and won't always execute immediately